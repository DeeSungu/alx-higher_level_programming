#include "lists.h"
/**
 * check_cycle - checks cycles
 * @list: list
 * Return: 1 0r 0
 */
int check_cycle(listint_t *list)
{
	listint_t *s = list;
	listint_t *f = list;

	if (list == 0)
	return (0);

	while (a && b && b->next)
	{
	ample = a->next;
	breaker = b->next->next;
	if (a == b)
	return (1);
	}

	return (0);
}
